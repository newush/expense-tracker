<p>*必填欄位</p>
<h2>請修改你的支出</h2>
<form action="/edit/{{ record._id }}" method="POST">
  <div class="form-group">
    <label for="category">名稱*：</label>
    <input class="form-control" type="text" placeholder="name" name="name" value="{{ record.name }}">
  </div>
  <div class="form-group">
    <label for="date-input">日期＊：</label>
    <input class="form-control" type="date" value="{{record.date}}" placeholder="date" name="date">
  </div>
  <div class="form-group">
    <label for="category">類別*：</label>
    <select required id="category" class="form-control" name="category">
      <option value="">請選擇類型</option>
      <option {{#ifeq "家居物業" record.category}} selected {{/ifeq}}>家居物業</option>
      <option {{#ifeq "交通出行" record.category}} selected {{/ifeq}}>交通出行</option>
      <option{{#ifeq "休閒娛樂" record.category}} selected {{/ifeq}}>休閒娛樂</option>
        <option{{#ifeq "餐飲食品" record.category}} selected {{/ifeq}}>餐飲食品</option>
          <option{{#ifeq "其他" record.category}} selected {{/ifeq}}>其他</option>
    </select>
  </div>
  <div class="form-group">
    <label for="date-input">金額＊：</label>
    <input class="form-control" type="text" placeholder="amount" name="amount" value="{{record.amount}}">
  </div>
  <div class="form-group justify-content-start">
    <button type="submit" class="btn btn-info">送出</button>
    <a href="/">
      <button type="button" class="btn btn-warning ml-3">返回</button>
    </a>
  </div>
</form>